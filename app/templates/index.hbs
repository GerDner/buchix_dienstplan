
<div class="row">
    <div class="centered"><span class="label label-default">Woche: {{kw}} | {{fullMo}} -> {{fullSo}}</span></div>
</div>
{{#link-to 'index' (query-params kw=prevKw year=prevYear) class="pull-left"}}<button class="btn btn-default">letzte Woche</button>{{/link-to}}
{{#link-to 'index' (query-params kw=nextKw year=nextYear) class="pull-right"}}<button class="btn btn-default">nächste Woche</button>{{/link-to}}


<table class="table table-striped table-hover">
    <thead>
        <tr>
            <td>Name</td>
            <td class="text-center">Mo {{mo}}</td>
            <td class="text-center">Di {{di}}</td>
            <td class="text-center">Mi {{mi}}</td>
            <td class="text-center">Do {{do}}</td>
            <td class="text-center">Fr {{fr}}</td>
            <td class="text-center">Sa {{sa}}</td>
            <td class="text-center">So {{so}}</td>
        </tr>
    </thead>
    <tbody>
        {{#if session.isAuthenticated}}
        {{#each type in controller}}
        <tr class="active"><td>{{type.value}}{{type.test}}</td><td></td><td></td><td></td><td></td><td></td><td></td><td></td></tr>
        {{#each person in type.person itemController="item"}}

        <tr>
            <td><button class="btn btn-xs btn-default" {{action "delete" person}}><span class="glyphicon glyphicon-remove"></span></button>
                <span {{action 'editPerson' person}} class="pointer">{{person.lastname}}, {{person.name}} {{person.currentWeek}}

                        {{#if person.phone}}{{view 'tooltip' data-original-title=person.phone glyphicon="glyphicon glyphicon-earphone"}}{{/if}}
                        {{#if person.handy}}{{view 'tooltip' data-original-title=person.handy glyphicon="glyphicon glyphicon-phone"}}{{/if}}
                        {{#if person.mail}}{{view 'tooltip' data-original-title=person.mail glyphicon="glyphicon glyphicon-envelope"}}{{/if}}
                    <div class="clearFix"></div>
                </span>
            </td>
            <td class="text-center">{{input class="field" focus-out="autoSave" value=person.cWeek.mo}}</td>
            <td class="text-center">{{input class="field" focus-out="autoSave" value=person.cWeek.di}}</td>
            <td class="text-center">{{input class="field" focus-out="autoSave" value=person.cWeek.mi}}</td>
            <td class="text-center">{{input class="field" focus-out="autoSave" value=person.cWeek.do}}</td>
            <td class="text-center">{{input class="field" focus-out="autoSave" value=person.cWeek.fr}}</td>
            <td class="text-center">{{input class="field" focus-out="autoSave" value=person.cWeek.sa}}</td>
            <td class="text-center">{{input class="field" focus-out="autoSave" value=person.cWeek.so}}</td>
        </tr>
        {{/each}}
        {{/each}}

        {{else}}









        {{#each type in controller}}
        <tr class="active"><td>{{type.value}}</td><td></td><td></td><td></td><td></td><td></td><td></td><td></td></tr>
        {{#each person in type.person itemController="item"}}
        <tr>
            <td>
                <span  class="pointer">{{person.lastname}}, {{person.name}}{{person.currentWeek}}
                    {{#if person.phone}}{{view 'tooltip' data-original-title=person.phone glyphicon="glyphicon glyphicon-earphone"}}{{/if}}
                    {{#if person.handy}}{{view 'tooltip' data-original-title=person.handy glyphicon="glyphicon glyphicon-phone"}}{{/if}}
                    {{#if person.mail}}{{view 'tooltip' data-original-title=person.mail glyphicon="glyphicon glyphicon-envelope"}}{{/if}}
                </span>
            </td>
            <td class="text-center">{{person.cWeek.mo}}</td>
            <td class="text-center">{{person.cWeek.di}}</td>
            <td class="text-center">{{person.cWeek.mi}}</td>
            <td class="text-center">{{person.cWeek.do}}</td>
            <td class="text-center">{{person.cWeek.fr}}</td>
            <td class="text-center">{{person.cWeek.sa}}</td>
            <td class="text-center">{{person.cWeek.so}}</td>
        </tr>
        {{/each}}
        {{/each}}
        {{/if}}
    </tbody>
</table>



{{#link-to 'index' (query-params kw=prevKw year=prevYear) class="pull-left"}}<button class="btn btn-default">letzte Woche</button>{{/link-to}}
{{#link-to 'index' (query-params kw=nextKw year=nextYear) class="pull-right"}}<button class="btn btn-default">nächste Woche</button>{{/link-to}}
